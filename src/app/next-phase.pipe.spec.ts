import { NextPhasePipe } from './next-phase.pipe';

describe('NextPhasePipe', () => {
  it('create an instance', () => {
    const pipe = new NextPhasePipe();
    expect(pipe).toBeTruthy();
  });
});
